---
# tasks file for Delete_AWS_EC2_instances
    - name: Terminate instances
      hosts: localhost
      tasks:
        - name: Terminate instances that were previously launched
          ec2:
            state: 'absent'
            instance_id: 'i-00470c16178632f94'